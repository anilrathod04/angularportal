# angularportal - Test Application

# Prerequisites - the pre-requisites for a development machine are:

*  nodejs
*  gulp installed globally
*  bower installed globally
*  node packages installed

# command to execute

npm install -g gulp
npm install -g bower
npm install
